# Database
DATABASE_URL="postgresql://user:password@localhost:5432/spendtracker?schema=public"

# Server
PORT=3000
NODE_ENV=development

# Optional: API key for securing endpoints (generate a random UUID)
API_KEY="your-secret-api-key-here"
